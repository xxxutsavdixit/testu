name: firstproject
on: workflow_dispatch
env:
   mypass: utsav
jobs:
   first:
     runs-on: ubuntu-latest
     steps:
          - name: This is my first code
            run: echo "${{env.mypass}}"

 
